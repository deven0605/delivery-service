spring:
  datasource:
    url: jdbc:h2:mem:deliverytestdb;DB_CLOSE_DELAY=-1;NON_KEYWORDS=USER
  jpa:
    hibernate:
      ddl-auto: create-drop

minio:
  # No real MinIO in the test context — MinioConfig swallows connection errors
  # at startup so contextLoads doesn't require a running instance.
  endpoint: http://localhost:9000
